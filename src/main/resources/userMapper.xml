<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ckw.permission.dao.UserDao">
	
	<select id="selectById" resultType="PlatformUserBean" parameterType="long">
		select * from woju_employee_base_info where id = #{id}
	</select>

</mapper>